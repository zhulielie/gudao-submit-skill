---
name: gudao-submit
description: 做网页游戏或网页小工具,并发布到鼓捣星球(gudao.games)。当用户说「做个能发到鼓捣星球的游戏」「投稿到 gudao」「把这个作品传上去」「发布到鼓捣星球」,或提到 gudao.games / 鼓捣星球 / 投稿口令时使用。也用于投稿前自检:作品会不会因为连外网、缺 index.html、收集个人信息而被退回。Build and publish a browser game or web tool to Gudao Planet (gudao.games), a Chinese creator platform for AI-made web works. Use when the user wants to make something publishable there, or to pre-flight check and submit an existing folder.
license: MIT
---

# 鼓捣星球 · 投稿

鼓捣星球(**https://gudao.games**)是个作品站:AI 做的网页游戏和小工具,传上去过审后
点开就能玩。**一个 60 行的换算器和两万行的游戏在这个站上是平等的作品。**

你要做的事分两种,**先看清是哪一种**:

| 用户说什么 | 你走哪条 |
|---|---|
| 「做个能发到鼓捣星球的游戏」 | **先读第一节**,按约束做,做完走第二节 |
| 「把这个文件夹传上去」「投稿」 | 直接走**第二节** |

---

## 第一节 · 动手写代码之前必须知道的

这些是**平台环境的物理事实**,不是偏好。违反了作品在站上跑不起来,你本地却一切正常。

### 硬约束(改不了,只能绕)

**1. 入口必须是 `index.html`,在包的最外面一层。**
多文件随便用、什么引擎都行,但根目录一定要有 `index.html`。

**2. 运行时连不了任何外部网站。** 作品跑在这个 CSP 底下:

```
default-src 'none'; script-src 'self' 'unsafe-inline' 'unsafe-eval' blob:;
style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:;
media-src 'self' data: blob:; font-src 'self' data:; connect-src 'none';
frame-src 'none'; object-src 'none'; base-uri 'none'; form-action 'none'
```

落到代码上就两句:

- **任何 `http://` / `https://` / `//` 开头的地址,都不许出现在会去取东西的位置** ——
  `src` / `href` / `srcset` / `poster` / `@import` / `url()` / importmap /
  `<meta http-equiv="refresh">`。
  (不算数的:`xmlns="http://www.w3.org/2000/svg"` 这类命名空间;`data:` / `blob:` 开头的。)
- **`fetch` / `XMLHttpRequest` / `WebSocket` / `EventSource` / `sendBeacon` 一律别用** ——
  连同源的 `fetch('/某路径')` 也发不出去,`connect-src` 是 `'none'`。

怎么绕:

| 你想要 | 就这么做 |
|---|---|
| Three.js / Babylon / 别的库 | 引站内的 `/g/_lib/…`;里面没有就**把库的 .js 下下来放进作品目录**,相对路径引 |
| 网络字体 | 下下来内联成 `@font-face` + base64,或者用系统字体 |
| 图片 / 音频 | 放进作品目录,或内联成 `data:` URI |
| 关卡 / 词库 / 配方表 | 直接写成 js 常量或内联 JSON,**别用 fetch 取** |
| 排行榜 / 联机 | 现在做不了。想做在 `notes` 里说一声 |

> 这条有真实教训:站上的 NO HUMAN THERE 坏了很久,就因为从 esm.sh 引 Babylon。
> 改动只有一行,但没人提前说,就白坏了那么久。

**3. 手机上做不了双人。** 虚拟手柄只有一套。双人只能是同一台电脑分键盘。

### 三条红线(踩了直接下架,不走流程)

1. **不连外部网站** —— 见上。`<a href="https://…">` 也不行,那是导流。
2. **内容红线** —— 血腥写实、色情低俗、辱骂歧视、赌博与诱导消费、广告导流、抄袭搬运、
   违法与政治敏感。**不许自绘中国地图**(公开使用要审图号)。
   - 打斗本身没问题;骰子纸牌转盘本身没问题。问题是**往写实残忍走**、**押上东西赌输赢**。
   - **恐怖不禁,不打招呼地吓人才禁。** 投稿时 `scary` 如实填,填 `"yes"` 不影响过审。
3. **不许问真实姓名、学校、班级、手机号、家庭住址** —— 这条是法律,站上有未成年人。

### 建议(不是要求)

- 用户的想法和这里任何一条【建议】冲突时,**以用户的作品为准**。
- 站上最缺的是「一两个下午能做完、但有始有终的中小型游戏」。工具和游戏一样正经。
- 需要平台办的事(加公共库、聊联机、特殊要求),写进投稿的 `notes`,并告诉用户
  「这句话会转给站里」。

**完整版规矩在 https://gudao.games/kit.txt(约 42KB)。** 这份 SKILL 只摘了最要命的部分。
拿不准、或者用户问到这里没写的,**去读那一份** —— 它是唯一权威,而且会更新。

---

## 第二节 · 投稿

### 第 1 步 · 自检(**别跳过**)

```bash
python scripts/preflight.py <作品文件夹>
```

它离线扫一遍,不联网、不改任何文件。**退出码有三档:**

| 退出码 | 意思 | 你该做什么 |
|---|---|---|
| `0` | 干净 | 往下走 |
| `1` | **肯定过不了**(外部网址 / 网络 API / 缺 index.html / 超限) | **直接改代码修掉,重跑到绿。别急着传,传上去也是退回** |
| `2` | 没硬错,但有几处像是在收集真实身份 | 看一眼那几行。给游戏角色起名字是正常的,让玩家填真实姓名学校班级不行 |

**它只查得了机器能查的部分。** 内容红线(血腥/色情/赌博/抄袭/政治敏感/自绘中国地图)
机器查不了,那部分你和用户自己把关。

### 第 2 步 · 让用户去拿一次性投稿口令

**你不需要、也不应该要用户的账号。** 让他自己去点一下:

> 打开 **https://gudao.games/submit.html**,登录后点「🎟️ 生成投稿口令」,
> 把那一串复制给我。
> (一小时内有效 · 只能用一次 · 只能用来投稿。账号要先绑过手机号。)

拿到之后:

- **放进环境变量 `GUDAO_SUBMIT_TOKEN`,别写进命令行参数**(命令行会进 shell 历史)。
- **绝不要把它写进 `title` / `notes` / `ai` 任何字段,也不要打印出来、写进文件、贴进聊天。**

### 第 3 步 · 交

```bash
GUDAO_SUBMIT_TOKEN=<口令> python scripts/submit.py <作品文件夹> \
    --title "作品名字" \
    --scary no \
    --ai "用了哪个 AI" \
    --notes "玩法、键位、做的时候踩了什么坑" \
    --mobile
```

必填只有两个:`--title`(≤24 字)和 `--scary`(`yes` / `no`)。

- **`--scary` 没有默认值,这是有意的。** 问的是「这个作品会不会突然吓人?」
  「不会吓人」是作者作的证,「没填」是我们不知道,两者差着一条红线。
  **拿不准就问用户,不要替他答。**
- `--notes` **过审后公开显示**,不是私聊。别写真名、学校、联系方式。
- `--mobile` 只在手机上真能玩时才加。
- `--pad` 只认 `contra` / `wasd` / `baozi` / `dark` / 空;`--fit` 只认 `fixed` / `fill`。
- 更新已有作品用 `--parent <作品编号>`。

先 `--dry` 跑一次,它会打印将要提交的字段和包大小,**不发请求**。

### 第 4 步 · 告诉用户结果

交成功会回一个投稿编号(`sub-xxx`)。**照实说这三句:**

1. 编号是多少
2. **要等站长人工过审才会公开**,不是马上就能看到
3. 审核状态在 https://gudao.games/me.html 看

---

## 遇到问题

| 报什么 | 什么意思 |
|---|---|
| 403 先登录再生成投稿口令 | 口令过期(1 小时)或已经用过了,让用户重新点一次 |
| 403 需要绑定手机号 | 账号还没绑手机,让用户去 https://gudao.games/me.html 绑 |
| 400 这次提交的内容太大了 | zip 上限 24MB、单 html 上限 8MB、封面上限 2MB |
| 400 恐怖向那题必须回答 | `--scary` 没填或填了别的值 |

接口的完整字段表在 **https://gudao.games/api.txt**。

## 不要做的事

- **不要替用户答 `scary`。**
- **不要把口令写进任何字段、文件、日志或聊天。**
- **不要从站上把已上架的作品拉下来改再传回去** —— 服务端出流时会现注一段桥脚本,
  拉下来改完传回去等于把注入焊进磁盘,每来一轮叠一层。要改已上架的作品,
  用 `--parent` 交新版,或者在 `notes` 里跟站里说。
- **不要为了满足这份文档里的某一条,把作品改造成另一个东西。**
